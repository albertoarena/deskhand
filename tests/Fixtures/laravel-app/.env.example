# Placeholder values only — this fixture is committed to a public repo
# (safety invariant #7). No real secrets or machine-specific paths.
APP_NAME="Deskhand Fixture"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://fixture.test

DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite

CACHE_STORE=file
SESSION_DRIVER=file
QUEUE_CONNECTION=database
